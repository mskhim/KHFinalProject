<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.payments.mapper.PaymentMapper">

    <!-- 특정 eventNo의 가격 조회 -->
    <select id="findPriceByEventNo" parameterType="int" resultType="int">
        SELECT price FROM event e join cart c on c.event_no=e.no WHERE c.no = #{id}
    </select>

    <!-- 결제 정보 저장 -->
    <insert id="insertPaymentRecord" parameterType="map">
        INSERT INTO payment (payment_id, total_amount, created_at)
        VALUES (#{paymentId}, #{totalAmount}, NOW())
    </insert>
</mapper>
